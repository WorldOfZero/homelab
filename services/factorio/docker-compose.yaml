x-logging-config:
  &logging-config
  driver: "json-file"
  options:
    max-size: "1m"
    max-file: "1"
    tag: "{{.Name}}"

services:
  factorio:
    image: factoriotools/factorio:2.1.20-rootless
    container_name: app-factorio
    restart: unless-stopped
    volumes:
      - '/mnt/Vault/Configuration/factorio/:/factorio'
    logging: *logging-config